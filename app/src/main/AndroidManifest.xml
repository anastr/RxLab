<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.anastr.rxlab">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.AboutActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".preview.OperationActivity" />

        <activity android:name=".ui.AlphabeticalOperatorsActivity" />
        <activity android:name=".ui.CreatingOperatorsActivity" />
        <activity android:name=".ui.FilteringOperatorsActivity" />
        <activity android:name=".ui.TransformingOperatorsActivity" />
        <activity android:name=".ui.CombiningOperatorsActivity" />
        <activity android:name=".ui.HotSourcesActivity" />
        <activity android:name=".ui.ComparingActivity" />
        <activity android:name=".ui.SchedulersActivity" />
    </application>

</manifest>